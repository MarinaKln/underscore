function isEven(arr) {
    for (var i = 0;i < arr.length;i++){
        if (arr[i] % 2 == 0) {
            predicated.push(arr[i]);
        } else {
            notPredicated.push(arr[i]);
        }
    }
}
